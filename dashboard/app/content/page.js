import ContentClient from './ContentClient';

export const metadata = {
  title: 'Content Library | PublishOps',
  description: 'Browse, filter, and manage all generated content across platforms with grid and list views.',
};

export default function ContentPage() {
  return <ContentClient />;
}
