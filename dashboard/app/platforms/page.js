import PlatformsClient from './PlatformsClient';

export const metadata = {
  title: 'Platform Rules | PublishOps',
  description: 'Comprehensive breakdown of algorithm signals, format specs, and optimization tips for each platform.',
};

export default function PlatformsPage() {
  return <PlatformsClient />;
}
