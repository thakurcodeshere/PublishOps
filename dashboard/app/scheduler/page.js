import SchedulerClient from './SchedulerClient';

export const metadata = {
  title: 'Upload Scheduler | PublishOps',
  description: 'Manage your content posting schedule with heatmap optimization and queue management.',
};

export default function SchedulerPage() {
  return <SchedulerClient />;
}
